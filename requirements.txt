langchain>=0.3.14
langgraph>=0.2.76
langchain-openai>=0.2.14
requests>=2.31.0
python-dotenv>=1.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
APScheduler>=3.10.0
SQLAlchemy>=2.0.0
cryptography>=42.0.0
urllib3>=2.0.0

# ── Optional: Memory / Retrieval backends ──────────────────────────────────────
# Install the package matching your chosen backend env var.
#
# MEMORY_BACKEND=postgres  or  RETRIEVAL_BACKEND=postgres
# psycopg2-binary>=2.9.0
#
# MEMORY_BACKEND=vector  or  RETRIEVAL_BACKEND=vector
# chromadb>=0.4.0
#
# ── Optional: Scheduler backends ──────────────────────────────────────────────
# SCHEDULER_BACKEND=celery
# celery[redis]>=5.3.0

# ── Optional: Crawl tools (RAG ingestion) ─────────────────────────────────────
# Strongly recommended for HTML parsing quality (crawl_and_index, crawl_recursive)
# beautifulsoup4>=4.12.0
